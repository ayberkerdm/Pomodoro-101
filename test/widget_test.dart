import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Replace with real tests once the app is built
    expect(true, isTrue);
  });
}
